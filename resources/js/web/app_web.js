// NPM modules
window.$ = window.jQuery = require('jquery');

$(function () {
    // custom modules
    require('../named_functions');

    // local modules
    require('./web');

    /*
     * COMMON STUFF
     **/

});
